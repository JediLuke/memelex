# TODO here we ought to be able to start, stop etc agents through an API
# Flamelex.Fluxus.action({__MODULE__, {:activate_agent, scene.assigns.state.tidbit}})
defmodule Memelex.Agent do
  
  def activate() do
    raise "not yet"
  end
end
